<h1><%= @workout.name %> Summary </h1>
<h5>Logged by: <%= link_to @workout.user.username, user_path(@workout.user) %></h5>
<ul>
    <li> 
        Date: <%= @workout.date_completed %>
    </li>

    <li>
        Duration: <%= @workout.duration %>
    </li>

    <li>
        Workout Type: <%= @workout.cardio_or_strength %>
    </li>

    <li>
        Average Heart Rate (bpm): <%= @workout.average_heart_rate %>
    </li>

    <li>
        Total Calories Burned: <%= @workout.total_calories %> 
    </li>

    <li>
        Description: <%= @workout.description %> 
    </li>
</ul>
<p><%= link_to 'View comments for this workout', workout_comments_path(@workout.id) %></p>

<p><%= link_to 'Leave a comment for this workout', new_workout_comment_path(@workout.id) %></p>

<% if current_user == @workout.user %>
<p><%= link_to 'Edit Workout', edit_workout_path %></p>
<p><%= link_to 'Delete Workout', workout_path(@workout), method: 'delete' %></p>
<% end %>
